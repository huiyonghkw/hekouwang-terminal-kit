# hekouwang-terminal-kit · Node via vfox (version-fox)
# Multi-language SDK manager. After activate: vfox add nodejs && vfox use nodejs@…
# Do not also load fnm / nvm / brew node in the same shell.
command -v vfox >/dev/null && eval "$(vfox activate zsh)"
